# LumaRijschool — Production npm config
# NOTE: overrides are in package.json (the ONLY place npm supports them)
# This .npmrc is for other npm settings only.
engine-strict=true
audit=false
fund=false